import { ErrorCardComponent } from "@lens-up/shared-components";
import AppContainer from "./AppContainer";

const NotFoundComponent = () => {
    return (
        <AppContainer>
            <ErrorCardComponent />
        </AppContainer>
    )
}

export default NotFoundComponent
